# GitHub Agent Environment Configuration Example
# Copy this file to .env.local or .env.dev and customize

# Environment type (local, dev, prod)
AGENT_ENV=local

# Logging level
LOG_LEVEL=DEBUG

# AWS Configuration (for dev/prod only)
# AWS_REGION=ap-southeast-2
# AWS_PROFILE=default

# GitHub OAuth Configuration (for dev/prod only)
# These are configured in AgentCore Identity, not needed locally
# GITHUB_CLIENT_ID=your_client_id
# GITHUB_CLIENT_SECRET=your_client_secret
